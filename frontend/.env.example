# URL base da API.
# Deixe vazio para usar o proxy do Vite (/api -> http://localhost:3333) em desenvolvimento.
VITE_API_URL="http://localhost:3333/api"

# Alvo do proxy do vite dev server (usado apenas quando VITE_API_URL nao esta definido)
VITE_PROXY_TARGET="http://localhost:3333"
